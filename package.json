{
  "name": "quicksum",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "node setup.js",
    "start-backend": "cd backend && npm start",
    "build-extension": "cd extension && npm run build",
    "dev": "concurrently \"npm run start-backend\" \"npm run build-extension\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "openai": "^5.11.0",
    "youtube-transcript": "^1.2.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
